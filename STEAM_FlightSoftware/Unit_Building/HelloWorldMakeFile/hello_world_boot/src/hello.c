#include "hello.h"
#include<stdio.h>

int hello(){
	/*FILE *fp;
 
	fp = fopen("/tmp/hello.txt", "w");

	if(fp == NULL) {
		printf("file can't be opened\n");
	        exit(1);
	}


	fprintf(fp, "Hello World\n");
	*/
	//while(1){
		printf("Hello world\n");
	//}
	//fclose(fp);
	return 0;

}

